<Window x:Class="EngineeringManagementApp.Views.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Autentificare"
        Height="560"
        Width="440"
        MinHeight="480"
        MinWidth="380"
        WindowStartupLocation="CenterScreen"
        ResizeMode="CanResizeWithGrip"
        Background="{DynamicResource BackgroundBrush}"
        FontFamily="{StaticResource AppFont}">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- Header band -->
        <Border Grid.Row="0"
                Background="{StaticResource PrimaryBrush}"
                Padding="32,28">
            <StackPanel>
                <TextBlock Text="Engineering Management"
                           FontSize="22"
                           FontWeight="Bold"
                           Foreground="White" />
                <TextBlock Text="Sistem de gestionare solicitări"
                           FontSize="13"
                           Foreground="#D6E6F8"
                           Margin="0,6,0,0" />
            </StackPanel>
        </Border>

        <!-- Login card -->
        <Border Grid.Row="1"
                Margin="28"
                Background="{StaticResource SurfaceBrush}"
                BorderBrush="{StaticResource BorderBrush}"
                BorderThickness="1"
                CornerRadius="10"
                Padding="28,32">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0"
                           Text="Conectare"
                           FontSize="20"
                           FontWeight="SemiBold"
                           Foreground="{StaticResource TextPrimaryBrush}"
                           Margin="0,0,0,24" />

                <StackPanel Grid.Row="1" Margin="0,0,0,16">
                    <TextBlock Text="Utilizator" Style="{StaticResource FieldLabelStyle}" />
                    <TextBox x:Name="LoginTextBox"
                             Style="{StaticResource TextBoxStyle}"
                             MaxLength="50"
                             KeyDown="Input_KeyDown" />
                </StackPanel>

                <StackPanel Grid.Row="2" Margin="0,0,0,8">
                    <TextBlock Text="Parolă" Style="{StaticResource FieldLabelStyle}" />
                    <PasswordBox x:Name="PasswordBox"
                                 Style="{StaticResource PasswordBoxStyle}"
                                 MaxLength="128"
                                 KeyDown="Input_KeyDown" />
                </StackPanel>

                <TextBlock x:Name="ErrorTextBlock"
                           Grid.Row="3"
                           TextWrapping="Wrap"
                           Foreground="{StaticResource ErrorBrush}"
                           FontSize="13"
                           Margin="0,8,0,0"
                           Visibility="Collapsed" />

                <Button Grid.Row="5"
                        x:Name="LoginButton"
                        Content="Autentificare"
                        Style="{StaticResource PrimaryButtonStyle}"
                        HorizontalAlignment="Stretch"
                        Margin="0,20,0,0"
                        Click="LoginButton_Click" />
            </Grid>
        </Border>
    </Grid>
</Window>
